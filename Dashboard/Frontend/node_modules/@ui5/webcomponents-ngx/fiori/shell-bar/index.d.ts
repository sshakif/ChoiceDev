import { EventEmitter } from '@angular/core';
import '@ui5/webcomponents-fiori/dist/ShellBar.js';
import { default as ShellBar, ShellBarAccessibilityAttributes, ShellBarLogoClickEventDetail, ShellBarMenuItemClickEventDetail, ShellBarNotificationsClickEventDetail, ShellBarProductSwitchClickEventDetail, ShellBarProfileClickEventDetail, ShellBarSearchButtonEventDetail } from '@ui5/webcomponents-fiori/dist/ShellBar.js';
import * as i0 from "@angular/core";
declare class ShellBarComponent {
    /**
          Defines the `primaryTitle`.
  
  **Note:** The `primaryTitle` would be hidden on S screen size (less than approx. 700px).
          */
    primaryTitle: string | undefined;
    /**
          Defines the `secondaryTitle`.
  
  **Note:** The `secondaryTitle` would be hidden on S and M screen sizes (less than approx. 1300px).
          */
    secondaryTitle: string | undefined;
    /**
          Defines the `notificationsCount`,
  displayed in the notification icon top-right corner.
          */
    notificationsCount: string | undefined;
    /**
          Defines, if the notification icon would be displayed.
          */
    showNotifications: boolean;
    /**
          Defines, if the product switch icon would be displayed.
          */
    showProductSwitch: boolean;
    /**
          Defines, if the Search Field would be displayed when there is a valid `searchField` slot.
  
  **Note:** By default the Search Field is not displayed.
          */
    showSearchField: boolean;
    /**
          Defines additional accessibility attributes on different areas of the component.
  
  The accessibilityAttributes object has the following fields,
  where each field is an object supporting one or more accessibility attributes:
  
  - **logo** - `logo.role` and `logo.name`.
  - **notifications** - `notifications.expanded` and `notifications.hasPopup`.
  - **profile** - `profile.expanded`, `profile.hasPopup` and `profile.name`.
  - **product** - `product.expanded` and `product.hasPopup`.
  - **search** - `search.expanded` and `search.hasPopup`.
  - **overflow** - `overflow.expanded` and `overflow.hasPopup`.
  
  The accessibility attributes support the following values:
  
  - **role**: Defines the accessible ARIA role of the logo area.
  Accepts the following string values: `button` or `link`.
  
  - **expanded**: Indicates whether the button, or another grouping element it controls,
  is currently expanded or collapsed.
  Accepts the following string values: `true` or `false`.
  
  - **hasPopup**: Indicates the availability and type of interactive popup element,
  such as menu or dialog, that can be triggered by the button.
  
  Accepts the following string values: `dialog`, `grid`, `listbox`, `menu` or `tree`.
  - **name**: Defines the accessible ARIA name of the area.
  Accepts any string.
          */
    accessibilityAttributes: ShellBarAccessibilityAttributes;
    /**
       Fired, when the notification icon is activated.
      */
    ui5NotificationsClick: EventEmitter<ShellBarNotificationsClickEventDetail>;
    /**
       Fired, when the profile slot is present.
      */
    ui5ProfileClick: EventEmitter<ShellBarProfileClickEventDetail>;
    /**
       Fired, when the product switch icon is activated.
  
  **Note:** You can prevent closing of overflow popover by calling `event.preventDefault()`.
      */
    ui5ProductSwitchClick: EventEmitter<ShellBarProductSwitchClickEventDetail>;
    /**
       Fired, when the logo is activated.
      */
    ui5LogoClick: EventEmitter<ShellBarLogoClickEventDetail>;
    /**
       Fired, when a menu item is activated
  
  **Note:** You can prevent closing of overflow popover by calling `event.preventDefault()`.
      */
    ui5MenuItemClick: EventEmitter<ShellBarMenuItemClickEventDetail>;
    /**
       Fired, when the search button is activated.
  
  **Note:** You can prevent expanding/collapsing of the search field by calling `event.preventDefault()`.
      */
    ui5SearchButtonClick: EventEmitter<ShellBarSearchButtonEventDetail>;
    private elementRef;
    private zone;
    private cdr;
    get element(): ShellBar;
    constructor();
    static ɵfac: i0.ɵɵFactoryDeclaration<ShellBarComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ShellBarComponent, "ui5-shellbar", ["ui5Shellbar"], { "primaryTitle": { "alias": "primaryTitle"; "required": false; }; "secondaryTitle": { "alias": "secondaryTitle"; "required": false; }; "notificationsCount": { "alias": "notificationsCount"; "required": false; }; "showNotifications": { "alias": "showNotifications"; "required": false; }; "showProductSwitch": { "alias": "showProductSwitch"; "required": false; }; "showSearchField": { "alias": "showSearchField"; "required": false; }; "accessibilityAttributes": { "alias": "accessibilityAttributes"; "required": false; }; }, { "ui5NotificationsClick": "ui5NotificationsClick"; "ui5ProfileClick": "ui5ProfileClick"; "ui5ProductSwitchClick": "ui5ProductSwitchClick"; "ui5LogoClick": "ui5LogoClick"; "ui5MenuItemClick": "ui5MenuItemClick"; "ui5SearchButtonClick": "ui5SearchButtonClick"; }, never, ["*"], true, never>;
    static ngAcceptInputType_showNotifications: unknown;
    static ngAcceptInputType_showProductSwitch: unknown;
    static ngAcceptInputType_showSearchField: unknown;
}
export { ShellBarComponent };
